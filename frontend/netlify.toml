[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build]
  command = "npm ci --prefix frontend && npm run build --prefix frontend"
  # IMPORTANT: do NOT set publish for Next.js

[[plugins]]
  package = "@netlify/plugin-nextjs"
